DEPLOYMENT INSTRUCTIONS

1) caulk-V3.8.html
   -> Copy it, rename the copy to "index.html"
   -> Upload to:  /caulk/index.html (overwrite existing)

2) images/ folder
   -> No changes to images in this update, included for completeness
   -> Goes inside /caulk/images/ (overwrite if prompted)

WHAT CHANGED IN V3.8:
All phone number instances (8 total across the page — header, hero,
quote form, footer, zip-checker result text, and JS error fallback)
changed from (973) 221-3464 to (848) 207-4572, per request, as a
temporary rollback until Zoom SMS/texting issue is resolved on the
973 number. Revert back to 973-221-3464 once that's fixed.
